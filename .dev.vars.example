# Copy to .dev.vars (read by `wrangler pages dev` and by `node dev.mjs`). Leave empty for MOCK mode.
# TypeSafe key, from https://console.typesafe.ai/keys
TYPESAFE_API_KEY=
# Optional: a separate secret for the signed chess state tokens (defaults to the API key)
STATE_SECRET=
