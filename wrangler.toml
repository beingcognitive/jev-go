# Local use only (`npm run cf:dev`). The live site is configured in the Cloudflare dashboard:
# build output "public", the D1 binding named DB, and secrets such as TYPESAFE_API_KEY and STATE_SECRET.
# Keep pages_build_output_dir out of this file: with it present, Cloudflare ignores dashboard bindings.
name = "jev-go"
compatibility_date = "2026-09-01"
